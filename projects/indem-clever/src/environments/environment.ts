/**
 * Configuración de entorno — Desarrollo.
 * apiUrl apunta al backend local (se conectará cuando exista).
 */
export const environment = {
  production: false,
  apiUrl: '/siniestros/api/v1',
  appName: 'CleverFlow',
  tokenKey: 'cf_token',
};
